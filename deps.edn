{:paths ["src"]
 :deps {
        ;; kotoba.io replaced clojure.java.io / java.io.File here (2026-09-24).
        ;; An explicit floor: the java.io.File method functions (exists?, file?,
        ;; parent-file, canonical-path, mkdirs, list-files ...) are in kotoba.io
        ;; from this sha, and tools.deps takes the NEWEST sha it is shown.
        io.github.kotoba-lang/io {:git/sha "f7b3fca8e08147ae7d658fe36ae0475a45fb76a9"}
        ;; kotoba.lang.text, not clojure.string. Pinned as an explicit floor:
        ;; tools.deps takes the NEWEST sha it is shown, so without a floor here
        ;; this repo silently rides whatever a sibling happens to name.
        io.github.kotoba-lang/text {:git/sha "73bdb13ae7a3d004b44bca08be03a3191157a38f"}
       io.github.kotoba-lang/langgraph
        {:git/sha "a332a770a0d2b5193f81b54483bb954fb29ef8d7"}
        ;; jp-go-digital-design-system (DADS, デジタル庁デザインシステム) --
        ;; this workspace's BASE design system. `jp-go-dds.skin/dds+skin`
        ;; is the compatibility skin written for exactly these
        ;; cloud-itonami operator consoles: it styles the small semantic
        ;; class vocabulary (.card/.badge/.bar/.ok/.warn/.critical/.muted)
        ;; the renderer emits, so this repo carries no design CSS of its
        ;; own. Consumed as a git dep (never :local/root) so a standalone
        ;; fork outside the monorepo still builds the console.
        io.github.kotoba-lang/jp-go-digital-design-system
        {:git/url "https://github.com/kotoba-lang/jp-go-digital-design-system"
         :git/sha "83db99546eabc53613ae826ea41d1533a3c574af"}}
 :aliases
 {:dev {:override-deps
        {io.github.kotoba-lang/langgraph
         {:local/root "../../kotoba-lang/langgraph"}
         io.github.kotoba-lang/langchain
         {:local/root "../../kotoba-lang/langchain"}}}
  :run {:main-opts ["-m" "apparelops.sim"]}
  ;; kbb -M:dev:render-html [out-file] -> drive the REAL actor
  ;; (apparelops.operation -> apparelops.governor -> apparelops.store) at
  ;; build time and write docs/samples/operator-console.html from what it
  ;; actually did. Deterministic (no clock / randomness / network):
  ;; re-running the renderer produces a byte-identical file.
  :render-html {:main-opts ["-m" "apparelops.render-html"]}
  :test {:extra-paths ["test"]
         :extra-deps {io.github.cognitect-labs/test-runner
                      {:git/tag "v0.5.1" :git/sha "dfb30dd6605cb6c0efc275e1df1736f6e90d4d73"}}
         :main-opts ["-m" "cognitect.test-runner"]
         :exec-fn cognitect.test-runner.api/test}
  :lint {:replace-deps {clj-kondo/clj-kondo {:mvn/version "2024.11.14"}}
         :main-opts    ["-m" "clj-kondo.main"
                        "--lint" "src" "test"
                        "--fail-level" "error"]}}}
